name: Test Workflow Dispatch

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-22.04
    steps:
      - name: Hello
        run: echo "Workflow jalan! Tes manual sukses."
      - name: Time
        run: date
